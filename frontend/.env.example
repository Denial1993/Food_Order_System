# 複製成 .env.local（Vite 的本機覆蓋檔，已在 .gitignore）
# cp .env.example .env.local

# 開發時不用填（Vite dev server proxy 自動轉到 localhost:8000）
# VITE_API_BASE_URL=

# 正式環境填你的 Render 後端 URL，例：
# VITE_API_BASE_URL=https://food-order-backend.onrender.com/api
